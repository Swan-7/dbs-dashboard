// import { initialState } from "../../appstate";

// export function userReducer(state = initialState, action) {
//   return {
//     user: action.type === "USER" ? action.user : state.user,
//   };
// }
